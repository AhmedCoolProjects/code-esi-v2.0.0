/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["ahmedcoolprojects.github.io", "firebasestorage.googleapis.com"],
  },
};

module.exports = nextConfig;
